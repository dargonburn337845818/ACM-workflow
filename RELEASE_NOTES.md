# ACM Workflow 0.22.0

VSCode 内 Codeforces 刷题全流程工作台：选题 → 题面翻译 → 测试 → 对拍 → 造数据 → 比赛 → 记录。

## 本次更新

### 工作流重构与性能
- **Dashboard 深模块**：记录面板展示数据统一由一个组合服务产出，减少重复读库与重复扫描。
- **题面渲染缓存**：Webview 题面 HTML、可翻译段落数增加进程内缓存，切换题面/编辑器不再重复 cheerio 解析。
- **CF 题集请求去重**：薄弱推荐、难度补全、历史图表并发触发时只请求一次 problemset。
- **题面打开更快**：难度补全改为后台异步执行，不再阻塞题面显示，前端通过 `statementDifficulty` 实时更新难度。
- **记录写入优化**：已存在的记录不再每次切换编辑器都整库持久化。

### AI 造数据脚本泛化修复
- **解析器三级兜底**：正文代码 → `reasoning_content` 代码块 → 裸 Python 暴力提取。
- **提示词锚定**：开头/结尾强约束输出格式，注入前 3 组样例作为格式锚点。
- **草稿-执行-报错-修正闭环**：脚本验证失败时把错误回喂 Spark，最多 3 次修正。
- **保底机制**：多次修正失败后写入可运行的最小骨架脚本，避免“换题无输出”导致整个流程卡死。

### 其他
- CF URL 解析统一收敛到 `parseCfProblemUrl`，覆盖 problemset / contest / gym。
- 冒烟测试扩展至 82 项，覆盖新增统计、题面缓存与 Spark 解析/提示词行为。

## 安装

下载 `acm-workflow-0.22.0.vsix`，在 VS Code 扩展面板选择「从 VSIX 安装」。

## License

MIT
