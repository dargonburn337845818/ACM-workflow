# ACM Workflow 0.24.4

VSCode 内 Codeforces 刷题全流程工作台：选题 → 题面翻译 → 测试 → 对拍 → 造数据 → 比赛 → 记录。

## 本次更新：样例兜底也应用真实范围

之前如果题面「输入格式」规则没有命中，会走“样例形状兜底”，但兜底里数值全部写死：

```python
random.randint(1, 10)
```

0.24.4 修复：

- 样例兜底也会从题面中提取 `a_i`、`u`、`v`、`x`、`y` 等范围并应用。
- 如果题面确实没有给范围，则使用 `1..10^9` 量级，不再全是 1..10。

### 示例

题面有：

```text
0 ≤ a_i ≤ 100
```

即使走样例兜底，也会生成：

```python
random.randint(0, 100)
```

不再出现：

```python
random.randint(1, 10)
```

## 原则

- 不调用 LLM、不加载模型。
- 优先级：输入格式 → 样例规律 → 最小骨架。
- 每一层都尽量把题面范围带入生成。

## 安装

下载 `acm-workflow-0.24.4.vsix`，在 VS Code 扩展面板选择「从 VSIX 安装」。

## License

MIT
