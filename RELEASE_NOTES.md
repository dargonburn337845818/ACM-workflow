# ACM Workflow 0.25.0

VSCode 内 Codeforces 刷题全流程工作台：选题 → 题面翻译 → 测试 → 对拍 → 造数据 → 比赛 → 记录。

## 本次更新：规则 + 本地 4B 分层生成

不再“因为 4B 小就不用 LLM”，而是把 LLM 放到正确的位置：

```text
输入格式 + 样例 + 约束
  ↓
规则模板命中？
  ├── 命中 → 毫秒级本地生成，不用 LLM
  └── 未命中 → 本地 4B Spark 生成
          ↓
      精简提示词（只给输入格式 + 样例 + 参考模板）
          ↓
      代码提取 + 运行/形状/范围验证
          ↓
      失败 → 修复 ≤3 次
          ↓
      仍失败 → 样例形状兜底
```

## 4B 小模型专项优化

### 提示词
- 不再塞整篇题面。
- 只给：
  - 题面「输入格式」
  - 数据范围
  - 1~3 个官方样例
  - 一段参考代码模板
- 明确要求：只输出 Python 代码。

### 采样参数
```text
temperature    0.2
top_p          0.9
top_k          20
repeat_penalty 1.05
max_tokens     1024
```

### 验证
- AST/运行检查
- 输出非空
- 形状符合输入格式
- 变量依赖自洽
- 范围落在约束内

## 其他保留
- 规则模板仍覆盖常见：数组、矩阵/边列表、范围约束。
- 样例形状兜底也会应用题面范围。
- `sparkAutoStart` 默认开启，但只在规则未命中时才拉起模型。

## 安装

下载 `acm-workflow-0.25.0.vsix`，在 VS Code 扩展面板选择「从 VSIX 安装」。

## License

MIT
