# ACM Workflow 0.24.0

VSCode 内 Codeforces 刷题全流程工作台：选题 → 题面翻译 → 测试 → 对拍 → 造数据 → 比赛 → 记录。

## 本次更新

### AI 生成脚本轻量化（回归初心）
- 不再让本地大模型“理解题面”。
- 「AI 生成脚本」改为「**按样例生成**」：
  - 根据官方样例的输入行数、每行 token 数、类型，直接生成随机化 Python 脚本。
  - 不调用 Spark、不加载模型。
  - 毫秒级完成，零模型上下文开销。
- 只有题目没有样例时，才生成最小骨架，并在界面明确提示。

### 本地部署参数大幅轻量化
- `sparkAutoStart` 默认 `false`，不再无谓拉起大模型。
- `ctx`：131072 → **8192**
- `batch`：512 → **256**
- `threads`：16 → **8**
- `GPU layers`：99 → **0（CPU 轻量）**
- `max_tokens`：4096 → **1024**
- 请求超时：300s → **60s**

Spark 仍保留为可选“复杂题面理解模式”，但默认路径已经完全不需要它。

### 其他
- 生成脚本默认保存到当前题目目录 `gen.py`，按题目隔离。
- 验证仍会检查“有输出且不是解释性废话”。
- 保底脚本改为“样例形状随机化”，比 `print(1)` 更实用。

## 安装

下载 `acm-workflow-0.24.0.vsix`，在 VS Code 扩展面板选择「从 VSIX 安装」。

## License

MIT
