# Changelog

最新版本：**0.22.0**（工作流性能重构 + AI 造数据脚本泛化修复）。
上一版本：**0.21.3**（Spark 本地模型集成 / 启动参数与无代码返回修复）。

完整迭代历史见 [docs/changelog.md](docs/changelog.md)：

- **0.22.0** 重构记录/题面/CF 题集性能：新增 Dashboard 组合服务，一次读库产出列表、统计、今日与历史图表；题面 viewHtml 与可翻译数缓存；CF problemset 请求并发去重；题面难度补全改后台异步并新增 `statementDifficulty` 前端更新；已存在记录不再重复整库持久化。修复 AI 造数据脚本“换题无输出”：解析器三级兜底（正文代码 / reasoning 代码块 / 裸 Python）、提示词首尾锚定与样例注入、验证失败后最多 3 次“报错-修正”闭环、多次失败写入保底可运行脚本。CF URL 解析统一收敛到 `parseCfProblemUrl`。
- **0.21.3** 修复 Spark 本地模型启动失败：当前 `D:\llama-spark\build\bin\llama-server.exe` 只接受短参数 `-ngl`，不接受 `--ngl`；已同步修正 `src/services/spark.ts` 与 `tools/start_spark.sh`。同时修复 AI 生成请求超时（默认 5 分钟、max_tokens 降到 4096）和“无代码返回”（回退提取 `reasoning_content`，并默认 `--reasoning off` 启动）；默认启动参数对齐 `startmain.ps1`：ctx 131072、threads 16、KV cache q4_0。
- **0.21.2** 集成本地 Spark 模型（`D:\llama\Spark-X2.5-4B-Q8_0` + `D:\llama-spark\build\bin\llama-server.exe`）：造数据页新增「AI 生成脚本」按钮，自动取当前题目题面让 Spark 生成 Python 造数据脚本；生成后自动运行验证、覆盖 `gen.py` 并插入/更新造数据流水线中的自定义脚本步骤；新增 `acmWorkflow.spark*` 全套配置；Spark 空闲 3 分钟自动停止释放显存、下次点击自动拉起；与本地翻译服务可共存且不抢占翻译；修复下拉框完全白色、鼠标拖动才显示内容的问题（`select`/`option` 深色配色）。
- **0.21.0** 对拍器升级为可组装：造数据改为「只流水线拼装」，去掉顶层预设类型；新增细粒度原语（单行单数 / 单个数 / 一行多个数 / 每行两个数 / 固定文本 / 换行 / 重复块），支持单行单数绑定变量、一行多个数/每行两个数直接引用变量当数量，傻瓜式拼装无需手动空格换行；多步精确拼接且每个步骤参数独立保留（换类型不再重置其他步骤数据）；比对方式支持 精确 / Token / 浮点误差 / Special Judge（外部 checker，`.cpp` / `.py` / `.js` / `.exe`）。新增与 [VSCode Background](https://github.com/caoge5524/vscode-background) 的联动：工作台「设为全局背景」可将当前壁纸一键应用到整个 VSCode。
- **0.20.5** 测试模块「样例」页改为适应性窗口：占满可用宽度，输入/期望输出随窗口自动单列或双列；用例框拉长（默认 120px，最大 480px / 60vh），查看更舒适。
- **0.20.4** 插件瘦身：移除工作台内「知识导论」模块，知识导论已拆分为独立本地小程序 `knowledge-ladder/`（PySide6 + Windows 便携 exe）；插件保持简洁，选题、测试、对拍、比赛、记录等核心功能不变。
- **0.20.3** 知识导论从 8 大分类 47 节点扩充到 10 大分类 99 节点，新增排序/前缀和/双指针/构造/位运算/树/矩阵/图匹配/2-SAT/SAM/EXCRT/线性基等缺失知识点；选题页新增 CF 官方标签 `string suffix structures`、`graph matchings`、`chinese remainder theorem`，全部中文显示。
- **0.20.2** 本地翻译默认模型改为 Ollama `hy-mt2:latest`（ID `ad76b286cab2`），诊断 / 配置引导 / 启动脚本同步识别；修复「题面」页下方仍显示样例运行区的问题，样例（编辑 / 运行）只出现在「样例」页。
- **0.20.1** 测试模块的「题面」与「样例」拆分为同一模块下的两个页面，移除原可拖动分栏；修复「题面」页仍重复展示样例的问题，官方样例统一在「样例」页以可编辑用例展示。
- **0.20.0** 题面 LaTeX 修复与排版优化；选题支持算法标签多选（OR 过滤）；服务门面化（深模块）代码库保留；本地翻译后端默认直接使用 Ollama `hy-mt2:q4`（端点 `http://127.0.0.1:11434`），按需自动拉起；移除文件切换器 / 学习路线 / 美化预设 / AtCoder / 牛客。
- **0.19.6** 修复比赛模块进入后只显示“加载中”但不自动请求的问题；新增“ACM Workflow: 环境配置引导”命令，检测本地翻译模型/服务，缺失时询问是否安装并提供国内镜像，失败时展示具体原因；移除一键提交功能。
- **0.19.5** 修复 Argos 本地翻译把数学占位符改写成 `{0}}`/`XQ0☃` 导致译文乱码的问题，改用 `MATH0` 占位符并兼容旧格式还原；题面区新增“正在翻译中…（已 Ns）”实时提示；题面小图加白底，深色主题下文字型图片不再黑字看不清。
- **0.19.4** 本地翻译服务自动启动增强：`start_local_translate.sh` 支持 Windows venv 的 `Scripts/python.exe` 布局。
- **0.19.3** 比赛题目列表的「翻译」按钮改为「打开题目」（浏览器打开 CF 原题）；题面工具栏「翻译」改为「重新获取」（强制重抓当前题面并更新本地缓存）；未开始比赛不再显示空榜单/空关注，改为「比赛尚未开始」提示；题面抓取后立即显示，翻译改为后台异步执行，本地翻译不可用时快速跳过，不再阻塞题面/卡 80s。
- **0.19.2** CF API 请求加固：5xx/非 JSON 自动重试，错误信息附带响应片段，减少比赛模块偶发报错；翻译手动/自动请求传入 ExtensionContext（DeepSeek 密钥可用），本地服务 Windows 自动启动改用 `bash.exe`，失败时输出具体 HTTP/响应体；诊断新增“翻译配置诊断”章节，检查本地服务/模型/配置；修复参数化 `start codeforces xxx` 未被识别为任务开始导致慢操作漏报。
- **0.19.1** 提交与导入 AC 历史增加已用时长反馈；导入按钮点击后立即禁用并显示“导入中…”，结束/失败自动恢复；实测本地离线 Argos 翻译约 0.8s 返回；MyMemory 当前可用（约 2s）但匿名免费额度有限（当天额度耗尽时会失败）；Google 在当前网络超时不可达；建议网络不稳/额度受限时改用 `local`。
- **0.19.0** 提交支持题目页右侧内联上传 cpp；比赛模块精简为只显示题目并保留缓存/刷新；记录每条可删除且“打开题目”跳转 CF 网页；本地翻译服务可复位重试。
- **0.18.9** 翻译按钮防重复发送，翻译中禁用并显示“翻译中…”；失败保留手动重试；修正每次刷新误显示“正在导入 AC 历史…”的问题。
- **0.18.8** 一键提交改为打开题目页并点击页面上的 Submit 链接，不再猜 `/contest/{id}/submit`，避免落到 CF 主页；支持 Gym 提交入口。
- **0.18.7** 网络诊断 curl 增加 `-4`，与扩展真实抓取一致，避免 hosts 规则下 IPv6 超时误报。
- **0.18.6** 修复一键提交直接打开对应 submit 页并在页内登录；提交页导航失败会重试并报当前 URL；提交浏览器自动使用 `acmWorkflow.proxy`；比赛参赛人数缓存 60s、并发提升到 4。
- **0.18.5** 修复 WebView 重复发送 `fetchProblem`；网络诊断 fetch 失败但 curl 成功时判为 OK。
- **0.18.4** 诊断改为实际轨迹异常：移除 Bug1-6 检查；网络诊断使用浏览器 UA 并支持代理；修复 diagnose 未记录结束；移除整个扩展洛谷支持。
- **0.18.3** 工作流诊断：新增 `diagnostics.ts`（轨迹环形缓存、Codeforces 直连探测、Bug1-6 检查、脱敏报告）；`环境诊断` 升级为 `工作流诊断`；工作台顶部「诊断」按钮；失败时提示运行诊断；测试与文档同步。
- **0.18.2** 题面公式修复与本地翻译工具：`.py` 造数据脚本使用 `python3`；本地离线翻译工具脚本；生成 `题面.md`；修复本地翻译公式占位符泄漏；修复 CF `$$$` 公式排版；记录/比赛直接使用 CF 登录态并移除手动绑定；文档补充。
- **0.18.0** 开源首版：修复 72 处编译错误完成模块化重组；删除全部硬编码个人路径（默认数据目录 `~/.acm-workflow`）；新增 ESLint / 冒烟测试 / walkthrough 入门指引 / README 与 docs 全套文档；隐私打包验证。
- **0.17.x** CF 网络加速（IPv4 直连 / 代理配置）、URL 导入、比赛一键创建拉取样例。
- **0.16.0** CF 登录态 / 比赛管理 / 造数据 / 对拍器 / 提交闭环 五大能力落地。
- **0.8.0** 架构重构：回归左侧工作台 + 原生编辑器，新增题面翻译面板。
- **0.5.1** 稳定版：快照式退出、CDN 多源回退、彻底清理。
